# Expected contract - typography-layout-auditor

The agent verifies DOCX package XML rather than trusting only the style manifest.

- Required Word entries exist.
- Styles are declared and referenced.
- Chapter new-page policy is proven by page breaks, section breaks, or chapter style.
- Page numbers are proven when the selected profile requires them.
