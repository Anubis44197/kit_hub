# Design Document Map

Map `novel-config.md` keys to concrete files:
- bootstrap
- character_core
- character_detail
- plot guides by episode range

Always resolve episode-specific mappings first, then fallback to common docs.
